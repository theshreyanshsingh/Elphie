{{- if and (eq .Values.exposure.mode "gatewayApi") .Values.ui.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "elphie.fullname" . }}-ui
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "elphie.labels" . | nindent 4 }}
spec:
  parentRefs:
    {{- if .Values.exposure.gatewayApi.createGateway }}
    - name: {{ include "elphie.fullname" . }}
    {{- else }}
    {{- range .Values.exposure.gatewayApi.parentRefs }}
    - name: {{ .name }}
      {{- if .namespace }}
      namespace: {{ .namespace }}
      {{- end }}
    {{- end }}
    {{- end }}
  {{- if .Values.exposure.gatewayApi.listenerHostname }}
  hostnames:
    - {{ .Values.exposure.gatewayApi.listenerHostname | quote }}
  {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "elphie.ui.fullname" . }}
          port: {{ .Values.ui.service.port }}
{{- end }}
